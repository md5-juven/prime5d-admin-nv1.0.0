export type GST = {
    email?: string;
    gst?: string;
    amount?: number
}

export type TDS = {
    email: string;
    amount: number;
    bill_id: string;
    createDate: string;
    tds: number;
    suserid: string;
    semail: string;
}